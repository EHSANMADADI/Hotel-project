"use client";
import Pagination from "@/components/shared/pagination";

const BlogsPagination = () => {
  return (
    <>
      <Pagination items={40} onClick={() => {}} perPage={4} />
    </>
  );
};
export default BlogsPagination;
